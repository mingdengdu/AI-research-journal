# 实验：S1S2 Baseline
date: 2025-01-30
author: mingdengdu
status: draft
tags: [system12, baseline]

## 假设（必须可证伪）
在控制条件 X 下，方法 A 能在任务 T 上将指标 M 提升至少 Δ（相对于基线 B）。

例：在翻译任务上，使用 method-A 能使 BLEU 提升至少 1.5。

## 背景与动机
简述为什么这个假设重要，引用相关工作或以前的实验结果。

## 可观测的预测
- 指标 M（例如 accuracy / BLEU / loss）将从 baseline 值 v0 增加到 >= v0 + Δ。
- 次要指标：训练时间、推理延迟、不变性测试等。

## 成功/失败准则（明确数值或检验方法）
- 成功：在 N 次独立运行中，平均 M >= v0 + Δ 且 p-value < 0.05（t-test / bootstrap）。
- 失败：未满足上述条件或出现回退/不可接受副作用（例如延迟翻倍）。

## 实验设计（可复现）
- 数据集：name/version，train/val/test 划分
- 代码入口：protocol.py（执行命令示例）
- 随机种子：列出及如何设置
- 超参搜索：网格/随机/贝叶斯，预算（如 20 runs）

## 测量与分析计划
- 记录哪些指标到 results.json（示例格式�� data/results.json）
- 统计检验方法和置信区间计算方式

## 风险与假设
列出主要风险、前置假设（如数据分布不变化、模型容量充足等）。

## 下一步
如果成功 -> 规模化/迁移到其他任务/写入论文草稿  
如果失败 -> 诊断实验（列出 3 个快速检查）